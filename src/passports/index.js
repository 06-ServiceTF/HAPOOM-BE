const googlePassport = require('./google.passport');
const instagramPassport = require('./naver.passport');
const kakaoPassport = require('./kakao.passport');
const localPassport = require('./local.passport');

module.exports = {
  googlePassport,
  instagramPassport,
  kakaoPassport,
  localPassport
};
